# frozen_string_literal: true

source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby '2.7.5'

# Web server (using WEBrick temporarily to avoid compilation issues)
# gem 'puma', '4.3.11'
# gem 'nio4r', '2.5.8'

# Development tools (commented out temporarily due to compilation issues)
# gem 'rubocop', '0.90.0', require: false

gem 'sinatra', '2.1.0'
